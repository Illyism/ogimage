import * as screenshot from "./screenshot";
import * as titleScreenshot from "./title-screenshot";
import * as overlayTitle from "./overlay-title";

export const generators = {
  screenshot: screenshot,
  "title-screenshot": titleScreenshot,
  "overlay-title": overlayTitle,
};
